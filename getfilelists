#!/bin/sh -x
# SPDX-License-Identifier: GPL-2.0-only

wget -N $1/repodata/repomd.xml
p=$(grep -o '[^"]*-filelists.xml.[a-z]*' repomd.xml)
wget --progress=dot:mega -nc $1/$p
n=$(basename $p)
ext=$(echo $n | sed 's/.*\.//')
ln -f $n filelists.xml.$ext
